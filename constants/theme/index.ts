import { radius, spacing } from "../design";
import { DARK_THEME } from "./dark";
import { LIGHT_THEME } from "./light";

export const theme = {
  dark: DARK_THEME,
  light: LIGHT_THEME,
  spacing,
  radius,
};
